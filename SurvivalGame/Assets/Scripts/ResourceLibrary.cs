using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceLibrary : MonoBehaviour {

   // ResourceManager rm;
    //Dictionary<string, int> resources;
    private void Start()
    {
       // rm.GetComponent<ResourceManager>();

    //    resources = rm.GetResourceCollection();
    }

    
}
